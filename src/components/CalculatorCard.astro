<!-- src/components/CalculatorCard.astro -->
---
export interface Props {
  class?: string;
}
const { class: customClass = '' } = Astro.props as Props;
---
<div class={`bg-surface-card border border-hairline p-5 md:p-6 shadow-sm min-w-0 ${customClass}`}>
  <slot />
</div>
